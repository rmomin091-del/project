# Development Prompts

This document contains a chronological record of the primary user requests used to build and configure the Internal Issue Tracker.

## Initial Setup
1. "Issue Tracker Database Schema. CREATE TABLE IF NOT EXISTS issues... CREATE TABLE IF NOT EXISTS comments..."
2. "my db update the env accordinly also check the latest terminal logs"
3. "my password is Rehan@123 for db"

## Optimization & Repository
4. "okay now go through the project and create a file in which put all the endpoints and optimised the project and with create a repo"
5. "and push the code with 2-3 commits not in one commit"
6. "the backend files in one commit and front end in another and the rest in another"
7. "also create a file as prompt.md which will contain all the prompt being used"
